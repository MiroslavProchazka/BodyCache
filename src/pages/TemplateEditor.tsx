import { EmptyState } from '../components/Workout/EmptyState'

export const TemplateEditor = () => {
  return (
    <EmptyState
      title="Template editor"
      description="Template editing UI and suggested set schemes are planned for milestone 2"
      actionLabel="Editor in M2"
    />
  )
}
