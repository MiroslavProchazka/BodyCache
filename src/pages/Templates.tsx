import { EmptyState } from '../components/Workout/EmptyState'

export const Templates = () => {
  return (
    <EmptyState
      title="Workout templates"
      description="Milestone 2 adds template create, ordering, and start workout from template"
      actionLabel="Templates in M2"
    />
  )
}
